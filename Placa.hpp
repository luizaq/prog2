#include <iostream>
#include <string>
using namespace std;

class Placa
{

private:
  string cor;
  string id;
  char modelo;// A antigo n brasil
public:
void setCor(string cor);
void setId(string id);
void setModelo(char modelo);
};
